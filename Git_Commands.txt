#Merge feature branch into gg_dev
git checkout gg_dev
git merge #branch --no-ff -m "[delivers #] " 


#Merge develop into gg_dev
git checkout develop
git pull origin develop
git rebase develop gg_dev


#if conflicts occur
git diff #show the conflicts

#make changes and add and continue
git add --all
git rebase --continue


#Merge gg_dev to develop
git checkout develop
git merge gg_dev --no-ff -m "Merged gg_dev into develop"
git push origin develop
git checkout gg_dev



